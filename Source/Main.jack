class Main
{
   function void main()
   {
      var RaceGame game;

      while (true)
      {
         do Screen.clearScreen();
         do Sys.wait(250);
         let game = RaceGame.new();
         do game.run();
         do game.dispose();
      }

      return; 
   }
}
